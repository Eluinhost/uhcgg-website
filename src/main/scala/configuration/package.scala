package object configuration {
  trait ServerHostConfig
  trait ServerPortConfig

  trait DatabaseConnectionStringConfig
  trait DatabaseUsernameConfig
  trait DatabasePasswordConfig

  trait RedditClientIdConfig
  trait RedditSecretConfig
  trait RedditRedirectUriConfig
}
